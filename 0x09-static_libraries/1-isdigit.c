#include "main.h"
/**
 * _isdigit - function checks if a letter is a digit
 * @c: function being checked
 *
 * Return: 1 if digit, 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
